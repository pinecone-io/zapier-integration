{
  "name": "pinecone",
  "version": "1.0.1",
  "description": "Vector database for AI applications with semantic search and retrieval capabilities",
  "main": "./dist/index.js",
  "scripts": {
    "test": "vitest",
    "clean": "rimraf ./dist ./build",
    "build": "npm run clean && tsc",
    "_zapier-build": "npm run build",
    "bootstrap": "lerna bootstrap --scope=$npm_package_name"
  },
  "dependencies": {
    "@pinecone-database/pinecone": "^6.1.1",
    "zapier-platform-core": "17.2.0"
  },
  "devDependencies": {
    "nock": "^14.0.5",
    "rimraf": "^5.0.10",
    "typescript": "5.6.2",
    "vitest": "^2.1.2"
  },
  "private": true
}
